/** Legacy relational data: matches, vendors, etc. (SQLite). */
export const SQLITE_CONNECTION = 'sqlite';

/** Users, bookings, finance, planner, events, notification logs (PostgreSQL). */
export const POSTGRES_CONNECTION = 'postgres';

/** Document store: profiles, chat messages/conversations (MONGODB_URI env). */
