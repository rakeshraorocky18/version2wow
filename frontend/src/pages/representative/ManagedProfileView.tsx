import Profile from '../Profile';

/** View the managed bride/groom matrimonial profile (separate from representative profile). */
export default function ManagedProfileView() {
  return <Profile managedMode />;
}
